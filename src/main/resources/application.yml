info:
  app:
    name: ${spring.application.name}

spring:
  application:
    name: div-cms
  resources:
    static-locations:

server:
  port: 4009

idam:
  api:
    url: ${IDAM_API_BASEURL:https://preprod-idamapi.reform.hmcts.net:3511}
    redirect-url: ${IDAM_API_REDIRECT_URL:https://div-pfe-aat.service.core-compute-aat.internal/authenticated}
  caseworker:
    username: mathanmathan@test.com
    password: mathan
  s2s-auth:
    microservice: ${AUTH_PROVIDER_SERVICE_CLIENT_MICROSERVICE:divorce_ccd_submission}
    totp_secret: ${AUTH_PROVIDER_SERVICE_CLIENT_KEY:DWYML5ONMS7KG44R}
    url: ${AUTH_PROVIDER_SERVICE_CLIENT_BASEURL:http://rpe-service-auth-provider-aat.service.core-compute-aat.internal}
    port: ${AUTH_PROVIDER_SERVICE_CLIENT_PORT:4502}
    health:
        context-path: ${AUTH_PROVIDER_SERVICE_CLIENT_HEALTH_CONTEXT_PATH:/health}

ccd:
  jurisdictionid: DIVORCE
  casetype: DIVORCE
  eventid:
    create: create
  server:
    port: 4452
    health:
      context-path: /status/health

core_case_data:
  api:
    url: ${CASE_DATA_STORE_BASEURL:http://ccd-data-store-api-aat.service.core-compute-aat.internal}

draft:
  delete:
    async: true
  store:
    api:
      baseurl: ${DRAFT_STORE_API_BASEURL:http://draft-store-service-aat.service.core-compute-aat.internal}
      document:
        type:
          divorceFormat: divorcedraft
          ccdFormat: divorcedraftccdformat
      max:
        age: 150
      encryption:
        key:
          template: "%s_%s"
          value: ${DRAFT_STORE_API_ENCRYPTION_KEY_VALUE:9VQKhID861AdxWWv1zlI}

case:
  formatter:
    service:
      api:
        baseurl: ${CASE_FORMATTER_SERVICE_API_BASEURL:http://div-cfs-aat.service.core-compute-aat.internal}

auth2:
  client:
    id: divorce
    secret: thUphEveC2Ekuqedaneh4jEcRuba4t2t

http:
  connect:
    timeout: 60000
    request:
      timeout: 60000

documentation:
  swagger:
    enabled: true

# GENERAL SPRING BOOT ACTUATOR CONFIG
# Context path for Spring Boot Actuator endpoints
# Allow actuator endpoints to be accessed without requiring authentication
# Enable default health indicators
# Enable disk space health check
management:
  endpoint:
    health:
      show-details: "ALWAYS"
      cache:
        time-to-live: 4000
  endpoints:
    web:
      base-path: /
      path-mapping:
        health: health
      exposure:
        include: info, health, metrics

logging:
  level:
    org.springframework.web: ERROR
    uk.gov.hmcts.reform.divorce.casemaintenanceservice: DEBUG
  pattern:
    console: "%d{yyyy-MMM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{15} - %msg%n"

azure:
  app_insights_key: ${APPINSIGHTS_INSTRUMENTATIONKEY:false}
